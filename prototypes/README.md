# Prototypes

Disposable interface studies live here. They use deterministic fixtures and are not deployed product surfaces.

- [`agent-call-orchestrator/`](agent-call-orchestrator/) — the interaction and layout study that informed the live session console.
